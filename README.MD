### lo.g

Log class that allows you to group logs by type, with pretty colours. Logs can be enabled/disabled by type.

## Install dependencies

 	$ sudo npm install

## Running the build/dev commands

 	Build the minified version

	$ npm run build

	Build the development files and watch folder for changes

	$ npm run watch

Built files go to the /dist/ folder

